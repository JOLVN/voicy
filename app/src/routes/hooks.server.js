// import '$lib/supabase'
// import { getSupabase } from '@supabase/auth-helpers-sveltekit'

// export const handle = async ({ event, resolve }) => {

//     const { session, supabaseClient } = getSupabase(event)

//     event.locals.supabase
// }